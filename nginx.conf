upstream client {
  server localhost:3000;
}

upstream api {
  server localhost:5500;
}

server {
  listen 80;

  location / {
    proxy_pass http://client;
  }

  location /api {
    rewrite /api/(.*) /$1 break;
    proxy_pass http://api;
  }

}
